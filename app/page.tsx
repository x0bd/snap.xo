import { useState, useRef } from "react";
import html2canvas from "html2canvas";
import AceEditor from "react-ace";

import "ace-builds/src-noconflict/mode-csharp";

const page = () => {
	return <div>I am building a ray.so clone</div>;
};

export default page;
